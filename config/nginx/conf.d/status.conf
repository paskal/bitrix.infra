server {
    listen 8084 default;
    server_name  _;
    server_name_in_redirect  off;
    location / {
        stub_status;          # print stats
        server_tokens on;     # print nginx version
        access_log   off;
   }
}
