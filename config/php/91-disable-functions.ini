; disable shell functions not used by any Bitrix module or cron job
; exec and proc_open are kept — needed by admin tools and acrit.export parallel processing
disable_functions = system,passthru,shell_exec,popen,pcntl_exec,pcntl_fork,pcntl_signal,pcntl_waitpid,pcntl_wexitstatus,dl
